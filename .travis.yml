language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update
  - mkdir antsbin
  - cd antsbin
install: 
  - sudo apt-get install -y cmake
script: 
  - cmake -DRUN_LONG_TESTS=OFF -DRUN_SHORT_TESTS=OFF ./.. && make
