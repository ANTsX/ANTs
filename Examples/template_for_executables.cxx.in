#include <iostream>
#include <vector>
#include <string>
#include "include/ants.h"

int main( int argc, char* argv[] )
{
  char* * ptr = &argv[1];

  return ants::@ANTS_FUNCTION_NAME@( std::vector<std::string>( ptr, ptr + argc - 1 ), &std::cout );
}
